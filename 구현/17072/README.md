💡 문제 핵심
한 줄에 3*m개의 값이 들어온다
이걸 RGB 묶음(3개씩)으로 처리해야 한다

👉 여기서 많이 틀림

⚠️ 내가 놓친 부분 (핵심)
1. 입력 구조 이해 부족

처음에 이렇게 접근했음:

for j in range(3*m):
    input()

👉 ❌ 잘못된 이유

입력은 “한 줄”에 다 들어옴
한 숫자씩 받는 게 아님

👉 ✅ 올바른 사고

row = list(map(int, input().split()))

2. RGB를 한 픽셀로 묶는 개념

처음에는 for y in x처럼 접근함

👉 ❌ 문제

y는 값이지 인덱스가 아님
픽셀 단위 처리가 안됨

👉 ✅ 핵심 개념
픽셀 하나 = 3칸

[R G B] [R G B] [R G B]
 0 1 2   3 4 5   6 7 8

 for j in range(m):
    r = x[3*j]
    g = x[3*j+1]
    b = x[3*j+2]



    4. 출력 형식
print("#")

👉 ❌ 문제

한 글자마다 줄바꿈
